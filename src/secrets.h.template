#ifndef SECRETS_H
#define SECRETS_H

// WiFi credentials - replace with your actual credentials
const char* ssid = "YOUR_WIFI_SSID";       // WiFi SSID to connect to
const char* password = "YOUR_PASSWORD";    // WiFi password

#endif // SECRETS_H
